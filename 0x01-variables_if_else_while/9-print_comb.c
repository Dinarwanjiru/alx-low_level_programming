#include <stdio.h>
/**
 * main - prints ll possible combinations of single-digit numbers
 * Return: Always
 */
int main(void)
{
int s;
for (s='0'; s <= '9'; s++)
{
putchar(s);
if (d != '9')
{
putchar( ',');
putchar(' ');
}
}
return (0);
}

