#include "main.h"
/**
 * Main -checks for uppercase character
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */
int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return(1);
	}
	return (0);
}
